import { StyleSheet } from 'react-native';
import colors from '@/styles/colors';
import fonts from '@/styles/fonts';

export const creditNameStyle = StyleSheet.create({
    creditName:{
        color: colors.black,
        textTransform: 'uppercase',
        fontSize: 18,
        fontFamily: fonts.text
    },
});